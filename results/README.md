# Results

`all_runs.csv` contains one row per completed experiment. `summary.csv` contains aggregate statistics across seeds. Generate figures and paper tables from these CSV files rather than copying values manually from logs or W&B.
